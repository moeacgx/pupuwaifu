// 集中配置：无需改 HTML / URL，即可切换数据源与默认页面 ID
// 把此文件的值改成你的远程仓库路径与页面 ID
// 例如：'https://raw.githubusercontent.com/<owner>/<repo>/main/pupuwaifu/data/pages/'

window.LINKS_CONFIG = {
  profileId: 'pupuwaifu',
  dataBase: 'https://raw.githubusercontent.com/ha534560/links-data/main/data/pages/',
  dataFile: 'links.json',
  // dataUrl: '', // 可直接指定完整 JSON 地址（优先级最高）
};
